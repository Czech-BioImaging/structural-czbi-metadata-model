# Installation

1. Have [pixi](https://pixi.prefix.dev/latest/) functional
1. `pixi install --frozen`

This should prepare the Python environment in which the [czbird package](../czbird-py)
is available.

# Playground

After calling `pixi shell`, a standard conda-like shell environment starts.
Try, e.g., `python` with `import czbird`...
